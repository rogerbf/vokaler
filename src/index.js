const all = ['a', 'e', 'i', 'o', 'u', 'y', 'å', 'ä', 'ö']

const front = ['i', 'y', 'long u', 'e', 'ä']

const raised = ['ö', 'short u', 'short a']

const retracted = ['o', 'å', 'long a']

module.exports = {
  all,
  front,
  raised,
  retracted
}
